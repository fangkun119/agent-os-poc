package com.agentos.web;

/**
 * Web 服务器门面（TS 7.1），第三周交付（TS 13）。
 *
 * <p>启动 Spring MVC 服务器，{@code agentos serve} 命令触发（TS 7.1 / 8.7），
 * 默认端口 8080，开启 Java 21 virtual thread（TS 7.1）。三种触发源（CLI / Web API /
 * AgentScheduler）汇入同一 AgentService.process，审计与 Session 语义一致（TS 2）。
 */
public class WebServer {

    // TODO: 实施阶段补 serve 启动入口与端口/virtual thread 配置（TS 7.1）
}
